# tbd later ... this is created to manage and execute operations on the server side ... *&&&*
preferably with go or cpython (c blobs after translation into a self contained binary ) this will be a persistant worker .. systemd service (but at a user level)

